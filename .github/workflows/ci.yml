name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Print nx version
        run: pnpm nx --version

      - name: Lint (affected)
        run: pnpm nx affected -t lint --parallel --base=origin/main

      - name: Test packages only (libraries)
        run: pnpm nx run-many -t test --projects="tag:nx:lib" --parallel

      - name: Test utilities with coverage
        run: pnpm nx test utilities -- --coverage --coverageReporters="text-summary,lcov"

      - name: Astro check (affected, exclude problematic projects)
        run: pnpm nx affected -t check --parallel --base=origin/main

      - name: Build (affected, exclude problematic projects)
        run: pnpm nx affected -t build --parallel --base=origin/main
