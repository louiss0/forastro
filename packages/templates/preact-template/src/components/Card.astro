---
import { getImage } from "astro:assets";
interface Props {
  title: string;
  src: string;
  alt?: string;
}

const { title, src, alt } = Astro.props;

const imageMetadata = await getImage({ src });
---

<div class="grid">
  <header>
    <img {...imageMetadata.attributes} alt={alt} />
  </header>
  <div class="py-2 px-4">
    <div class="text-xl">{title}</div>
  </div>
  <footer class="">
    <slot />
  </footer>
</div>
